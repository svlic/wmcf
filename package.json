{
  "name": "wavemonitor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
    "check": "npm --prefix worker run check && npm --prefix frontend run typecheck",
    "test": "npm --prefix worker test && npm --prefix frontend test",
    "dev": "npm run build && npm --prefix worker run dev",
    "deploy": "npm --prefix worker ci --include=dev && npm --prefix worker run deploy"
  }
}
